{
  "name": "survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed": "node seed.js",
    "start": "node server.js",
    "setup": "npm install && npm run seed"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0"
  }
}
